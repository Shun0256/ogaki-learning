# 開発ログ

## 方針
- 今後コードを書いた際は、変更内容・確認結果・次の作業をこのファイルに細かく記録する。
- 1件の作業ごとに、対象ファイル・変更内容・検証結果・備考を残す。
- 仕様変更やトラブル解決時は、原因と対応内容も記載する。

## 2026-07-02
- 画面全体のデザインを教材らしく自然に見えるように調整した。
- イントロ画面・地図表示・マーカー詳細モーダルの CSS を更新した。
- 右側の調査状況パネルを追加し、各マーカーの調査完了状態を `済/未` で表示するようにした。
- `奥の細道むすびの地記念館` の項目がパネル内で詰まって表示される問題を、ステータスリストの折り返しとレイアウト修正で解消した。
- `src/home.css` の CSS 構文エラー（`status-item span` ブロックの閉じ忘れ）を修正し、`npm run build` で正常にビルドできることを確認した。
- ビルド確認は `npm run build` で成功した。

## 2026-07-03
### マップレイアウト調整
- **対象ファイル**: `src/components/LessonMapView.vue`
- **変更内容**:
  - `#map` を `.map-container` でラップし、マップ表示幅を狭めた（`width: calc(100% - 56px)`、`max-width: 960px`）。
  - 左右の不要な空白領域を削除しマーカーに影響しない幅制御を実装。
  - マップの高さを縦長に変更（`height: min(78vh, 920px)`、`min-height: 560px`、`max-height: 920px`）。
  - マップ初期表示の余白を調整（`markerBounds.pad(0.03)`、`fitBounds` の `padding: [40, 40]`）。
  - 左右の視覚的な狭まりは疑似要素オーバーレイで追加（`::before`、`::after`）。
- **検証**: レイアウト変更により、地図表示がコンパクトになり、不要な左右余白が減少。

### 新規マーカーの追加
- **対象ファイル**: `src/mapMarkers.js`
- **変更内容**:
  - 新規マーカー2件を追加・更新:
    - `funamoto-minato`（船町港跡）: 画像 `/img/minato.jpg`、タイトル「水運で栄えた大垣の玄関口」
    - `kagano-hachiman`（加賀野八幡神社）: 画像 `/img/yahata-jinja.jpg`、タイトル「地中から湧き出る大垣の名水」
  - 各マーカーに詳細情報を追加（title、explanation、sections）
  - セクション構成: `概要`、`見どころ`、`豆知識`

### 不要なセクション削除
- **対象ファイル**: `src/mapMarkers.js`
- **変更内容**: すべてのマーカーの不要な「見出し」セクションを削除（大垣城、四季の広場、奥の細道むすびの地記念館、水門川、スイトピアセンター）。
  - 船町港跡・加賀野八幡神社は元々見出しがないため変更なし。

### 次の推奨作業
- `npm run dev` で動作確認（マップレイアウト、新規マーカー表示、モーダル詳細確認）
- 画像ファイルが正常に読み込まれているか確認
- 変更をコミット: `git add src/mapMarkers.js src/components/LessonMapView.vue && git commit -m "Adjust map layout, add marker details (2026-07-03)"`

# 実績の実装テンプレ
imgファイル内の画像名：
実績タイトル：
実績解説：
実績ウィンドウ表示後の追加解説：

## 2026-07-24
### 初回訪問チュートリアル導線の実装
- **対象ファイル**:
  - `src/components/FirstVisitTutorial.vue`（新規）
  - `src/components/LessonMapView.vue`
  - `src/Home.vue`
- **変更内容**:
  - 初回訪問時のみ表示されるチュートリアルパネルを追加。
  - チュートリアルの進行を以下4段階で表示:
    - マップクリック
    - ミニクイズ正解
    - 「理解した」チェック
    - 確認問題
  - 進行判定は既存の学習データ（`openedDetailCount` / `miniQuizCorrectCount` / 理解チェック数 / `learningCompleted`）に連動。
  - チュートリアルを閉じた状態は `localStorage` に保存し、2回目以降は非表示。
  - 導入イントロ画面も初回のみ表示（`lesson-intro-seen` で管理）。

### 品質確認
- `npm run build` を実行し成功。
- `LessonMapView` 内の `mapMarkers` 動的 import 警告を、静的 import 統一で解消。

### チュートリアルUIのアプリ風モーダル化
- **対象ファイル**:
  - `src/components/FirstVisitTutorial.vue`
- **変更内容**:
  - 右下の小型パネルを、画面中央の大型オーバーレイモーダルへ変更。
  - ステップ進行に合わせてカードが横スライドするUIへ変更。
  - 閉じる操作を簡単化:
    - 右上の大きい閉じるボタン
    - 背景クリックで閉じる
    - フッターの「あとで閉じる」ボタン
- **検証**:
  - `npm run build` 成功。

### 再訪時のチュートリアル再表示（テスト用）
- **対象ファイル**:
  - `src/components/LessonMapView.vue`
- **変更内容**:
  - 調査状況パネルに「チュートリアルを見る」ボタンを追加。
  - 既に初回チュートリアル完了済みでも、手動で再表示できるようにした。
- **検証**:
  - `npm run build` 成功。

### チュートリアルのスライド操作追加とスマホ表示改善
- **対象ファイル**:
  - `src/components/FirstVisitTutorial.vue`
- **変更内容**:
  - 前へ/次へボタンを追加し、スライドを手動で移動できるようにした。
  - 「現在の進捗へ戻る」ボタンを追加し、学習進捗に同期し直せるようにした。
  - モーダルに `max-height` と `overflow-y: auto` を追加し、縦が狭い端末での重なり・はみ出しを防止。
  - ヘッダーの閉じるボタンを絶対配置にして、タイトルとの重なりを防止。
  - 手順リストを2カラムグリッド（モバイルは1カラム）に変更し、文字重なりを防止。
- **検証**:
  - `npm run build` 成功。

### チュートリアル不要要素の削除
- **対象ファイル**:
  - `src/components/FirstVisitTutorial.vue`
- **変更内容**:
  - 5項目目の「チュートリアル完了」スライドを削除し、4ステップ構成に整理。
  - 「現在の進捗へ戻る」ボタンを削除。
  - 「あとで閉じる」ボタンの文言を「閉じる」に変更。
- **検証**:
  - `npm run build` 成功。